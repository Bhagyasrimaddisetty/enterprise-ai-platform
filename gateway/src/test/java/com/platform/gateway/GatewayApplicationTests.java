package com.platform.gateway;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class GatewayApplicationTests {

    @Test
    void contextLoads() {
        // Verifies the gateway's route configuration and Spring context wire
        // up correctly — most valuable test for a routing-only service.
    }
}
